1) All data can fit in memory.
2) All fields but lastname and firstname are nullable.
3) Assumes that a given file uses only one of the delimiters throughout the file.
4) Assumes that a file will not contain any of the delimiters in the data.
5) Empty files are invalid
6) Assume it's okay to ignore empty lines in files
7) Assume it's okay to ignore empty files
